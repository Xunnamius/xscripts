{{> header}}

{{#if noteGroups}}
  {{~#each noteGroups}}

    {{#if @root.isPatch~}}
      ####
    {{~else~}}
      ###
    {{~/if}} 💥 {{title}} 💥

    {{#each notes~}}
      * {{text}}{{#unless @last}}
    {{/unless}}
    {{~/each}}
  {{~/each}}
{{~/if}}

{{~#each commitGroups}}
  {{#if title}}

    {{#if @root.isPatch~}}
      ####
    {{~else~}}
      ###
    {{~/if}} {{title}}

  {{/if}}
  {{~#each commits}}
    {{~> commit root=@root}}
  {{/each}}
{{~/each}}

