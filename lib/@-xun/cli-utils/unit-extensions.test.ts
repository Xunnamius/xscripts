describe('::withStandardBuilder', () => {
  it('accepts custom builder function and object', async () => {
    expect.hasAssertions();
  });

  it('reconfigures yargs-parser with more sensible defaults', async () => {
    expect.hasAssertions();
  });

  it('enableVersionOption adds "version" to common options group', async () => {
    expect.hasAssertions();
  });

  it('silent, quiet, and hush properly imply one-another', async () => {
    expect.hasAssertions();
  });
});
