describe('::makeStandardConfigureExecutionContext', () => {
  it('returns expected context by default', async () => {
    expect.hasAssertions();
  });

  it('returns expected context when withListr2Support is enabled', async () => {
    expect.hasAssertions();
  });
});

describe('::makeStandardConfigureErrorHandlingEpilogue', () => {
  it('returned function behaves as expected when silenced', async () => {
    expect.hasAssertions();
  });

  it('returned function behaves as expected when quieted', async () => {
    expect.hasAssertions();
  });

  it('returned function behaves as expected when hushed', async () => {
    expect.hasAssertions();
  });

  it('returned function behaves as expected when the error message is empty', async () => {
    expect.hasAssertions();
  });

  it('returned function behaves as expected when the native error has a cause different than its message', async () => {
    expect.hasAssertions();
  });

  it('returned function behaves as expected when receiving a chain of sub-error causes', async () => {
    expect.hasAssertions();
  });

  it('returned function behaves as expected when receiving more than MAX_LOG_ERROR_ENTRIES sub-error causes', async () => {
    expect.hasAssertions();
  });

  it('returned function behaves as expected when listr2support is enabled', async () => {
    expect.hasAssertions();
  });
});
